/**
 * Hook for task scheduling logic (e.g. today's tasks, dependencies).
 * Stub for now.
 */
export function useTaskScheduler(): void {
  // TODO: wire to useTaskStore.getTodaysTasks, dependency checks
}
